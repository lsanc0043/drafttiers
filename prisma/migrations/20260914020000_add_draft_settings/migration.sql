CREATE TYPE "DraftType" AS ENUM ('SNAKE', 'LINEAR', 'CUSTOM');

ALTER TABLE "Board"
  ADD COLUMN IF NOT EXISTS "teamCount" INTEGER,
  ADD COLUMN IF NOT EXISTS "draftPosition" INTEGER,
  ADD COLUMN IF NOT EXISTS "roundCount" INTEGER,
  ADD COLUMN IF NOT EXISTS "draftType" "DraftType",
  ADD COLUMN IF NOT EXISTS "roundTimerSeconds" INTEGER,
  ADD COLUMN IF NOT EXISTS "fantasyScoring" JSONB;
